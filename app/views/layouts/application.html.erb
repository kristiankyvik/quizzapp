<!DOCTYPE html>
<html>
<head>
  <title>Quizzapp</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag "http://js.pusherapp.com/1.9/pusher.min.js"%>
  <%= csrf_meta_tags %>
</head>
<body>
<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand"  href=<%= root_path %>>Quizzapp</a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
        <ul class="nav navbar-nav navbar-right">
          <p class="navbar-text">Logged in as <strong><%= current_user.email %></strong></p> 
          <li class="active"> <%= link_to "Logout", destroy_user_session_path, method: :get, :class => 'navbar-link'  %></strong></li>
          <li><a href=<%= user_quizzes_path(current_user) %>>Profile</a></li>
        </ul>
        <% else %>
        <li class="active pull-right"> <%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link'  %></li>
        <li class="active pull-right"> <%= link_to "Login", new_user_session_path, :class => 'navbar-link'  %></strong></li>
        <% end %>
      </ul>

      </div>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
  


<div class="container">

<%= yield %>

</div>

  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p> 
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= yield :head %>
</body>
</html>
